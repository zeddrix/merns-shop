# README screenshots

Captured locally from the dev stack (http://localhost:5020) for GitHub README marketing.

Regenerate:

```bash
docker compose up -d mongo
pnpm db:seed
pnpm dev
pnpm readme:screenshots
```
